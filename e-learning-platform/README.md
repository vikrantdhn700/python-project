# E-Learning Platform

A dependency-free Python project demonstrating OOP and inheritance.

- `User` is the parent class.
- `Student` and `Instructor` inherit from `User`.
- Instructors create and display courses.
- Students enroll in courses and track their progress.

## Run

```powershell
python main.py
```
